import React from 'react';
import styles from './SidebarButton.module.css'

export default function SidebarButton() {
    return <div className={styles.sidebarButtonContainer}>
        <div className={styles.sidebarButtonBox}>
            HELLO
        </div>
    </div>
}